// src/constants.ts — exports nommés UNIQUEMENT

// Diamètre + espacement (pas de gap vertical) — pas de répétition center-to-center
export const BEAD_DIAMETER_MM = 5
// Rayon pour dessin SVG/PDF (cercle 4mm physique)
export const BEAD_RADIUS_MM = 2
export const DEFAULT_ROWS = 26
export const DEFAULT_COLS_ODD = 22
export const DEFAULT_COLS_EVEN = 21
